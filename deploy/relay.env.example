# Copy to /etc/hermes-bridge/relay.env and set mode 0600.
# Generate a token, for example: openssl rand -hex 32
HERMES_BRIDGE_ADMIN_TOKEN=replace-with-at-least-24-random-characters
HERMES_BRIDGE_STATE_DIR=/var/lib/hermes-bridge
PORT=8080
